#include "main.h"

/**
 * _isdigit - function that multiplies two integers
 *@c:The character to be checked
 * Return: 1 for digit character or 0 for anytining else
 */

int _isdigit(int c)

{
if (c >= 48 && c <= 57)
{
return (1);
}
return (0);
}
