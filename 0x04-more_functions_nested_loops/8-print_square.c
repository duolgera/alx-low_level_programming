#include "main"

/*
 * print_square -prints a square, followed by a new line
 * @n:Num of times the squre is printed
 * Return: Always 0 (Success)
 */
void print_square(int size)
{
	int i, j;
	for (n <= 0)
	{
		_putchar(' ');
	}
	for (i = 0; i)
