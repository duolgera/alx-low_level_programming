#include <stdio.h>
/**
 * main - A program that prints a line using puts fuction
 * return:0 (we have done with success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
